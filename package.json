{
  "name": "ruq-website-mockup",
  "version": "0.1.0",
  "private": true,
  "description": "Redesign base for riseupqueens.com — static build, content extracted from the live site",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs && npm run css && node scripts/build-nav-options.mjs && node scripts/build-nav-hero.mjs && node scripts/build-nav-tune.mjs && node scripts/build-nav-visibility.mjs && node scripts/build-nav-scroll.mjs && node scripts/build-nav-glass.mjs && node scripts/build-hero.mjs",
    "css": "tailwindcss -c tailwind.config.cjs -i src/styles/tailwind.css -o dist/styles.css --minify",
    "dev": "node scripts/build.mjs && npm run css -- --watch",
    "serve": "node scripts/serve.mjs",
    "fetch:assets": "node scripts/fetch-assets.mjs"
  },
  "devDependencies": {
    "tailwindcss": "3.4.17"
  },
  "engines": {
    "node": ">=20"
  }
}
